import java.util.Scanner;

@SuppressWarnings("unused")
public class app {
    public static void main(String[] args){
        // ControladorDeEstoque controlador = new ControladorDeEstoque();
        // ManipularArquivo manipulador = new ManipularArquivo("produtos.csv", controlador);
        // manipulador.carregarProdutos();
        // controlador.excluirProduto(1001);
        // manipulador.salvarArquivo(controlador.getProdutos());
        // manipulador.imprimirDados();

        // Scanner scanner = new Scanner(System.in);
        // Verificador verificador =  new Verificador();
        // ControladorDeEstoque controlador = new ControladorDeEstoque();
        // ManipularArquivo manipulador = new ManipularArquivo("produtos.csv");
        // String opcao = "n";
        // while(opcao.equals("n")){
        // controlador.aumentarLista(controlador.adicionarProduto());
        // System.out.println("Quer sair do programa? s/n");
        // verificador.verificarResposta(opcao = scanner.nextLine());
        // }
        // manipulador.salvarArquivo(controlador.getProdutos());
        // manipulador.imprimirDados();
        // scanner.close();
    }
}
